spring:

  datasource:
    url: jdbc:postgresql://localhost:5435/intervention_db
    username: intervention_user
    password: intervention_password


  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true